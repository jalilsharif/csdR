% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{residualize_expression}
\alias{residualize_expression}
\title{Residualize expression against confounders}
\usage{
residualize_expression(x, covariates)
}
\arguments{
\item{x}{Numeric matrix, samples in rows and genes in columns.}

\item{covariates}{Numeric matrix or data.frame with the same number of rows
as \code{x}. Factors are expanded via \code{model.matrix}.
An intercept is always included.}
}
\value{
Numeric matrix of the same dimensions as \code{x}, with column names
preserved.
}
\description{
For each gene (column), fit a linear model on the supplied
covariates and replace the expression with the residuals. This is the
standard way to remove batch / purity / library-size effects before
co-expression analysis.
}
\examples{
set.seed(1)
x <- matrix(rnorm(50), nrow = 10, ncol = 5)
colnames(x) <- paste0("g", 1:5)
batch <- rep(1:2, each = 5)
residualize_expression(x, covariates = data.frame(batch = factor(batch)))
}
